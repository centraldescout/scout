export function calculateTeamFit(player:any,teamStyle='possession'){const tacticalFit=teamStyle==='possession'?Math.round((player.metrics.passing+player.metrics.attack)/2):Math.round((player.metrics.defending+player.metrics.physical)/2);const squadFit=Math.round((player.rating+player.percentile)/2);const ageCurveFit=player.age<26?85:65;const financialFit=player.marketValue<8000000?88:62;const fitScore=Math.round((tacticalFit+squadFit+ageCurveFit+financialFit)/4);return{fitScore,tacticalFit,squadFit,ageCurveFit,financialFit,recommendation:fitScore>80?'Strong fit':'Context-dependent fit'}}
