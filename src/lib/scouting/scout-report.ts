export function generateScoutReport(player:any){const strengths=[];const weaknesses=[];if(player.metrics.attack>70)strengths.push('High attacking output');if(player.metrics.passing>70)strengths.push('Strong progression and passing');if(player.metrics.physical>70)strengths.push('Elite physical profile');if(player.metrics.defending<55)weaknesses.push('Defensive impact below average');if(player.age>29)weaknesses.push('Lower resale horizon');return{summary:`${player.fullName} profiles as a ${player.position} with strong upside in ${player.club}.`,strengths,weaknesses,tacticalFit:'Fits proactive possession-based systems with aggressive pressing.',developmentRisk:player.age<21?'Needs development time':'Moderate adaptation risk',resalePotential:player.age<24?'High':'Medium'}}
