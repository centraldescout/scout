export function calculatePercentile(value:number,values:number[]){const sorted=[...values].sort((a,b)=>a-b);const index=sorted.findIndex(v=>v>=value);return Math.max(1,Math.round((index/sorted.length)*100))}
export function buildPositionPercentiles(players:any[],position:string,metric:string,minMinutes=600){const scoped=players.filter(p=>p.position===position&&p.minutes>=minMinutes);const values=scoped.map(p=>Number(p.metrics?.[metric]||p[metric]||0));return scoped.map(player=>({...player,percentile:calculatePercentile(Number(player.metrics?.[metric]||player[metric]||0),values)}))}
