export function calculateMonchiScore(player:any){const ageScore=Math.max(0,100-(player.age-18)*3);const minutesScore=Math.min(100,player.minutes/30);const attack=(player.metrics.attack||0)*0.2;const passing=(player.metrics.passing||0)*0.15;const defending=(player.metrics.defending||0)*0.15;const physical=(player.metrics.physical||0)*0.15;const obv=(player.rating||0);const resale=ageScore*0.15;return Math.round(ageScore*0.2+minutesScore*0.15+attack+passing+defending+physical+obv*0.15+resale)}}