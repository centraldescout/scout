import {metricRegistry} from '@/lib/metrics/metric-registry'
export function PercentileBars({player}:{player:any}){const metrics=metricRegistry.filter(metric=>metric.percentileEnabled).slice(0,6);return(<div style={{background:'#18181b',padding:24,borderRadius:18,border:'1px solid #27272a'}}><h3 style={{marginTop:0}}>Percentile Profile</h3><div style={{display:'grid',gap:16}}>{metrics.map(metric=>{const value=Math.min(100,Math.round((player.percentile||65)-(Math.random()*12)));return(<div key={metric.slug}><div style={{display:'flex',justifyContent:'space-between',marginBottom:6}}><span>{metric.displayName}</span><strong>{value}</strong></div><div style={{height:10,borderRadius:999,background:'#27272a'}}><div style={{width:`${value}%`,height:'100%',borderRadius:999,background:'#22c55e'}}/></div></div>)})}</div></div>)}