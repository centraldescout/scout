import Link from 'next/link'
import { players } from '@/data/mock'
import { findSimilarPlayers } from '@/lib/scouting/similarity'
export default function ReplacementPage(){const target=players[0];const similar=findSimilarPlayers(target,players);return(<div><h1 style={{fontSize:42}}>Replacement Engine</h1><div style={{background:'#18181b',padding:24,borderRadius:18,marginTop:24}}><h2>Replacing: {target.fullName}</h2><p>{target.position} · {target.club}</p></div><div style={{display:'grid',gridTemplateColumns:'repeat(auto-fit,minmax(260px,1fr))',gap:18,marginTop:24}}>{similar.map(player=><Link key={player.id} href={`/players/${player.id}`} style={{background:'#18181b',padding:20,borderRadius:18,border:'1px solid #27272a'}}><strong>{player.fullName}</strong><p style={{color:'#a1a1aa'}}>{player.club} · {player.position}</p><p>Similarity: {Math.round(player.similarity*100)}%</p><p>Age: {player.age}</p><p>Value: €{(player.marketValue/1000000).toFixed(1)}M</p></Link>)}</div></div>)}