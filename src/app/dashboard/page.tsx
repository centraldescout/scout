import { StatCard } from '@/components/dashboard/stat-card'
const stats=[{label:'Players',value:50},{label:'Clubs',value:4},{label:'Competitions',value:2},{label:'Seasons',value:3}]
export default function DashboardPage(){return(<div><h1 style={{fontSize:42,marginBottom:24}}>Dashboard</h1><p style={{color:'#a1a1aa',marginBottom:24}}>Professional football scouting workspace.</p><div style={{display:'grid',gridTemplateColumns:'repeat(auto-fit,minmax(220px,1fr))',gap:20}}>{stats.map((stat)=><StatCard key={stat.label} label={stat.label} value={stat.value} />)}</div></div>)}