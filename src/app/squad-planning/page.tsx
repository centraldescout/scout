import { players } from '@/data/mock'
export default function SquadPlanningPage(){const avgAge=Math.round(players.reduce((sum,p)=>sum+p.age,0)/players.length);const grouped=Object.groupBy(players,(player:any)=>player.position);return(<div><h1 style={{fontSize:42}}>Squad Planning</h1><p style={{color:'#a1a1aa'}}>Long-term roster planning and succession analysis.</p><div style={{display:'grid',gridTemplateColumns:'repeat(auto-fit,minmax(220px,1fr))',gap:18,marginTop:24}}><div style={card}><h3>Average Age</h3><strong style={value}>{avgAge}</strong></div><div style={card}><h3>High Risk Contracts</h3><strong style={value}>6</strong></div><div style={card}><h3>Succession Needs</h3><strong style={value}>RB · ST</strong></div></div><div style={{display:'grid',gridTemplateColumns:'repeat(auto-fit,minmax(260px,1fr))',gap:18,marginTop:24}}>{Object.entries(grouped).map(([position,group])=><div key={position} style={card}><h3>{position}</h3><p style={{color:'#a1a1aa'}}>Depth chart</p><div style={{display:'grid',gap:10}}>{group?.slice(0,4).map(player=><div key={player.id} style={{padding:12,borderRadius:12,background:'#09090b'}}><strong>{player.fullName}</strong><div style={{color:'#a1a1aa'}}>{player.age} yrs · {player.status}</div></div>)}</div></div>)}</div></div>)}
const card={background:'#18181b',padding:20,borderRadius:18,border:'1px solid #27272a'}
const value={fontSize:34}
